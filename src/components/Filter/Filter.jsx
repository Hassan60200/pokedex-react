import React from 'react';

const Filter = ({search}) => {
    console.log(search)
    return (
        <div>
            {search}
            je suis un filtre
        </div>
    );
};

export default Filter;